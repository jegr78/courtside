courtside:
  bootstrap-admin:
    username: bootstrap-admin
    password: temporary-password
    display-name: Bootstrap Administrator

server:
  servlet:
    session:
      cookie:
        secure: false

spring:
  datasource:
    hikari:
      maximum-pool-size: 4
      minimum-idle: 0

logging:
  level:
    org.hibernate.orm.jdbc.error: ERROR
