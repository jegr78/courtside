courtside:
  mail:
    host: localhost
    port: 2525
    from: no-reply@courtside.test
    reply-to: board@courtside.test
  bootstrap-admin:
    username: bootstrap-admin
    password: temporary-password
    display-name: Bootstrap Administrator

server:
  servlet:
    session:
      cookie:
        secure: false

spring:
  modulith:
    moments:
      enabled: false
  datasource:
    hikari:
      maximum-pool-size: 4
      minimum-idle: 0

logging:
  level:
    org.hibernate.orm.jdbc.error: ERROR
