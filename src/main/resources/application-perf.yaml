courtside:
  performance:
    confirm-disposable: ${COURTSIDE_PERF_CONFIRM_DISPOSABLE:false}
    shared-password: ${COURTSIDE_PERF_SHARED_PASSWORD:}

spring:
  datasource:
    url: ${COURTSIDE_DB_URL:jdbc:postgresql://db:5432/courtside_perf}
