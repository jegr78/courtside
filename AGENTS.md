# Courtside agent instructions

`CLAUDE.md` is the canonical instruction file for every coding agent in this repository. Read and
follow it in full before changing the repository. This indirection keeps Codex and Claude on one
shared policy instead of maintaining two copies that can drift.
