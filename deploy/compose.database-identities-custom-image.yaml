services:
  database-setup:
    image: ${COURTSIDE_CUSTOM_IMAGE_REPOSITORY:?set COURTSIDE_CUSTOM_IMAGE_REPOSITORY in .env}@sha256:${COURTSIDE_CUSTOM_IMAGE_DIGEST:?set COURTSIDE_CUSTOM_IMAGE_DIGEST in .env}

  database-migrate:
    image: ${COURTSIDE_CUSTOM_IMAGE_REPOSITORY:?set COURTSIDE_CUSTOM_IMAGE_REPOSITORY in .env}@sha256:${COURTSIDE_CUSTOM_IMAGE_DIGEST:?set COURTSIDE_CUSTOM_IMAGE_DIGEST in .env}
