services:
  app:
    volumes:
      - ${COURTSIDE_DB_TLS_AUTHORITY:?set COURTSIDE_DB_TLS_AUTHORITY in .env}:/etc/courtside/database-authority.pem:ro
    environment:
      COURTSIDE_DB_TLS_MODE: verify-full
      COURTSIDE_DB_TLS_ROOT_CERTIFICATE: /etc/courtside/database-authority.pem
